version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=sqlite:///test_dashboard.db
      - SECRET_KEY=your-docker-secret-key-123
    volumes:
      - .:/app
    restart: unless-stopped